Description of the project:

How to use the project (incremental updates):
use GCC to compile driver.cr: "gcc driver.c -o driver"
Then execute driver expliciting the file to convert and the flags to use:
-v: activates the verbose mode;
-o: prints the name of the output file.